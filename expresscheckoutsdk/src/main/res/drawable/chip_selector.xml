<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Selected state -->
    <item android:state_selected="true">
        <shape android:shape="rectangle">
            <stroke android:width="1dp" android:color="@color/black_text" /> <!-- Selected border color -->
            <solid android:color="@color/button_disabled_EAEDF0" /> <!-- Selected background color -->
            <corners android:radius="32dp" />
        </shape>
    </item>

    <!-- Default state -->
    <item>
        <shape android:shape="rectangle">
            <stroke android:width="1dp" android:color="@color/black_text_12" /> <!-- Default border color -->
            <solid android:color="@color/white" /> <!-- Default background color -->
            <corners android:radius="32dp"  />
        </shape>
    </item>
</selector>
